# Slide Bank por Tipología

El banco de slides (.pptx por tipología narrativa) NO viene empaquetado para mantener
el plugin liviano. Se regenera ejecutando:

```
python skills/generate-deck/scripts/extract_typologies.py <deck.pptx> \
    --output-dir slide_bank/
```
